<%= form_for @user, url: portal_path(@user), html: { method: :put } do |f| %>
  First name: <%= f.text_field :first_name %>
  Last name: <%= f.text_field :last_name %>
  Email: <%= f.email_field :email, :value => "email@email.com" %> 
  <%= f.submit %>
<%end%>